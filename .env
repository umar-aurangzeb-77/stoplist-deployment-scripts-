# =============================================================
#         Stoplist Project Environment Configuration
# =============================================================
# -- Server IP --
# The public IP address of the server where this is deployed.
SERVER_IP=your_server_ip_here


# -- PostgreSQL Database (External) --
# The hostname or IP address of your external PostgreSQL server.
DB_HOST=your_external_db_host

# The port your external PostgreSQL server is running on (default is 5432).
DB_PORT=5432

# The name of the database the backend will use.
DB_DATABASE=stoplist_prod_db

# The username for the database.
DB_USERNAME=stoplist_user

# The password for the database.
DB_PASSWORD=your_strong_database_password


# -- Backend Application --
# A secret key used for signing JSON Web Tokens (JWTs).
# Generate a long, random string for this value.
JWT_SECRET=your_super_secret_jwt_key_that_is_very_long


# -- Nginx --
# Nginx container name
NGINX_CONTAINER_NAME=stoplist_nginx
